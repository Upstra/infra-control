import { SetupProgressMapper } from './setup-progress.mapper';
import { SetupStatusMapper } from './setup-status.mapper';

export const SetupMappers = [SetupProgressMapper, SetupStatusMapper];

export { SetupProgressMapper, SetupStatusMapper };
