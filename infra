#!/bin/bash

# Script de gestion de l'infrastructure Infra Control
# Usage: ./infra [command]

set -e

# Couleurs pour l'affichage
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Fichiers et répertoires
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Détection automatique de l'environnement
if [ -f ".env.rasp" ] && [ -f "/proc/device-tree/model" ] && grep -q "Raspberry Pi" "/proc/device-tree/model" 2>/dev/null; then
    ENV_FILE=".env.rasp"
    echo -e "${BLUE}🍓 Raspberry Pi détecté - Utilisation de .env.rasp${NC}"
elif [ -f ".env.prod" ]; then
    ENV_FILE=".env.prod"
    echo -e "${BLUE}🚀 Mode production - Utilisation de .env.prod${NC}"
else
    ENV_FILE=".env"
    echo -e "${YELLOW}⚠️  Mode développement - Utilisation de .env${NC}"
fi

COMPOSE_FILE="docker-compose.prod.yml"

# Vérifier que le fichier .env existe
check_env() {
    if [ ! -f "$ENV_FILE" ]; then
        echo -e "${RED}❌ Fichier $ENV_FILE non trouvé!${NC}"
        echo "Créez-le à partir du template approprié"
        exit 1
    fi
}

# Afficher l'aide
show_help() {
    echo "Usage: ./infra [command]"
    echo ""
    echo "Commands:"
    echo "  deploy        - Déploiement complet avec restauration de backup"
    echo "  start         - Démarre tous les services"
    echo "  stop          - Arrête tous les services"
    echo "  restart       - Redémarre tous les services"
    echo "  status        - Affiche l'état des services"
    echo "  logs          - Affiche les logs (suivis en temps réel)"
    echo "  backup        - Crée une sauvegarde de la base de données"
    echo "  restore       - Restaure une sauvegarde"
    echo "  migrate       - Exécute les migrations de base de données"
    echo "  clean         - Nettoie les conteneurs et volumes (ATTENTION: supprime les données)"
    echo "  setup-local   - Installe ou configure PostgreSQL / Redis en local (hors Docker)"
    echo "  help          - Affiche cette aide"
    echo ""
    echo "Examples:"
    echo "  ./infra deploy          # Déploiement initial"
    echo "  ./infra start           # Démarrage normal"
    echo "  ./infra logs            # Voir les logs"
    echo "  ./infra setup-local     # Configurer les services en local"
}

deploy() {
    check_env
    echo -e "${BLUE}🚀 Déploiement complet de l'infrastructure...${NC}"
    echo ""
    
    if ! command -v docker &> /dev/null; then
        echo -e "${RED}❌ Docker n'est pas installé!${NC}"
        exit 1
    fi
    
    echo -e "${YELLOW}📁 Création des dossiers...${NC}"
    mkdir -p backups logs
    
    echo -e "${YELLOW}🔨 Construction des images Docker...${NC}"
    docker-compose -f $COMPOSE_FILE --env-file $ENV_FILE build
    
    echo -e "${YELLOW}🗄️  Démarrage de PostgreSQL et Redis...${NC}"
    docker-compose -f $COMPOSE_FILE --env-file $ENV_FILE up -d postgres redis
    
    echo -e "${YELLOW}⏳ Attente du démarrage des services (15s)...${NC}"
    sleep 15
    
    if ls backups/*.sql 1> /dev/null 2>&1; then
        echo -e "${YELLOW}📥 Restauration de la base de données...${NC}"
        LATEST_BACKUP=$(ls -t backups/*.sql | head -1)
        echo "   Utilisation de: $LATEST_BACKUP"
        docker exec -i infra-control-postgres psql -U postgres postgres < "$LATEST_BACKUP"
        
        if [ $? -eq 0 ]; then
            echo -e "${GREEN}✅ Base de données restaurée!${NC}"
        else
            echo -e "${RED}❌ Erreur lors de la restauration${NC}"
            echo -e "${YELLOW}   Continuons avec une base vierge...${NC}"
        fi
    else
        echo -e "${YELLOW}⚠️  Aucune backup trouvée - Démarrage avec base vierge${NC}"
    fi
    
    echo -e "${YELLOW}🎯 Démarrage du backend...${NC}"
    docker-compose -f $COMPOSE_FILE --env-file $ENV_FILE up -d backend
    
    echo -e "${YELLOW}📊 Démarrage des services de monitoring...${NC}"
    docker-compose -f $COMPOSE_FILE --env-file $ENV_FILE up -d prometheus grafana node-exporter || echo -e "${YELLOW}   Monitoring optionnel non démarré${NC}"
    
    echo -e "${YELLOW}⏳ Attente du démarrage complet (30s)...${NC}"
    sleep 30
    
    echo -e "${BLUE}🔍 Vérification finale...${NC}"
    if curl -s http://localhost:8080/health/simple > /dev/null 2>&1; then
        echo -e "${GREEN}✅ Backend opérationnel!${NC}"
        IP=$(hostname -I | awk '{print $1}')
        echo ""
        echo -e "${GREEN}✨ Déploiement terminé avec succès!${NC}"
        echo ""
        echo -e "${BLUE}📌 Accès aux services:${NC}"
        echo "   - API Backend: http://$IP:8080"
        echo "   - Documentation Swagger: http://$IP:8080/docs"
        echo "   - Prometheus: http://$IP:9090"
        echo "   - Grafana: http://$IP:3001 (admin/admin)"
    else
        echo -e "${RED}❌ Le backend ne répond pas!${NC}"
        echo "   Vérifiez les logs avec: ./infra logs backend"
        exit 1
    fi
}

start() {
    check_env
    echo -e "${BLUE}🚀 Démarrage de l'infrastructure...${NC}"
    docker-compose -f $COMPOSE_FILE --env-file $ENV_FILE up -d
    sleep 10
    if curl -s http://localhost:8080/health/simple > /dev/null 2>&1; then
        echo -e "${GREEN}✅ Infrastructure démarrée${NC}"
    else
        echo -e "${YELLOW}⚠️  Le backend met du temps à démarrer...${NC}"
        echo "   Vérifiez dans 30 secondes ou consultez les logs"
    fi
}

stop() {
    echo -e "${YELLOW}🛑 Arrêt de l'infrastructure...${NC}"
    docker-compose -f $COMPOSE_FILE --env-file $ENV_FILE down
    echo -e "${GREEN}✅ Infrastructure arrêtée${NC}"
}

restart() {
    echo -e "${YELLOW}🔄 Redémarrage de l'infrastructure...${NC}"
    docker-compose -f $COMPOSE_FILE --env-file $ENV_FILE restart
    echo -e "${GREEN}✅ Infrastructure redémarrée${NC}"
}

status() {
    echo -e "${BLUE}📊 État des services:${NC}"
    docker-compose -f $COMPOSE_FILE --env-file $ENV_FILE ps
    echo -e "\n${BLUE}🔍 Tests de santé:${NC}"
    if curl -s http://localhost:8080/health/simple > /dev/null 2>&1; then
        echo -e "  Backend: ${GREEN}✅ OK${NC}"
    else
        echo -e "  Backend: ${RED}❌ KO${NC}"
    fi
    if docker exec infra-control-postgres pg_isready > /dev/null 2>&1; then
        echo -e "  PostgreSQL: ${GREEN}✅ OK${NC}"
    else
        echo -e "  PostgreSQL: ${RED}❌ KO${NC}"
    fi
    if docker exec infra-control-redis redis-cli ping > /dev/null 2>&1; then
        echo -e "  Redis: ${GREEN}✅ OK${NC}"
    else
        echo -e "  Redis: ${RED}❌ KO${NC}"
    fi
}

logs() {
    SERVICE=$2
    if [ -z "$SERVICE" ]; then
        echo -e "${BLUE}📜 Logs de tous les services (Ctrl+C pour quitter):${NC}"
        docker-compose -f $COMPOSE_FILE --env-file $ENV_FILE logs -f --tail=100
    else
        echo -e "${BLUE}📜 Logs de $SERVICE (Ctrl+C pour quitter):${NC}"
        docker-compose -f $COMPOSE_FILE --env-file $ENV_FILE logs -f --tail=100 $SERVICE
    fi
}

backup() {
    check_env
    echo -e "${BLUE}💾 Création d'une sauvegarde...${NC}"
    mkdir -p backups
    TIMESTAMP=$(date +%Y%m%d_%H%M%S)
    BACKUP_FILE="backups/backup_${TIMESTAMP}.sql"
    docker exec infra-control-postgres pg_dump -U postgres postgres > $BACKUP_FILE
    if [ $? -eq 0 ]; then
        echo -e "${GREEN}✅ Sauvegarde créée: $BACKUP_FILE${NC}"
    else
        echo -e "${RED}❌ Erreur lors de la sauvegarde${NC}"
        exit 1
    fi
}

restore() {
    check_env
    echo -e "${BLUE}📥 Restauration d'une sauvegarde...${NC}"
    if [ ! -d "backups" ] || [ -z "$(ls -A backups/*.sql 2>/dev/null)" ]; then
        echo -e "${RED}❌ Aucune sauvegarde trouvée dans backups/${NC}"
        exit 1
    fi
    echo "Sauvegardes disponibles:"
    select BACKUP_FILE in backups/*.sql; do
        if [ -n "$BACKUP_FILE" ]; then
            break
        fi
    done
    echo -e "${YELLOW}⚠️  ATTENTION: Cette opération va écraser la base actuelle!${NC}"
    read -p "Continuer? (oui/non): " confirm
    if [ "$confirm" != "oui" ]; then
        echo -e "${YELLOW}❌ Restauration annulée${NC}"
        exit 0
    fi
    docker exec -i infra-control-postgres psql -U postgres postgres < $BACKUP_FILE
    if [ $? -eq 0 ]; then
        echo -e "${GREEN}✅ Base restaurée depuis: $BACKUP_FILE${NC}"
    else
        echo -e "${RED}❌ Erreur lors de la restauration${NC}"
        exit 1
    fi
}

migrate() {
    echo -e "${BLUE}🔄 Exécution des migrations...${NC}"
    docker exec infra-control-backend pnpm migration:run
    if [ $? -eq 0 ]; then
        echo -e "${GREEN}✅ Migrations exécutées${NC}"
    else
        echo -e "${RED}❌ Erreur lors des migrations${NC}"
        exit 1
    fi
}

clean() {
    echo -e "${RED}⚠️  ATTENTION: Suppression complète des données!${NC}"
    read -p "Êtes-vous sûr? (oui/non): " confirm
    if [ "$confirm" != "oui" ]; then
        echo -e "${YELLOW}❌ Nettoyage annulé${NC}"
        exit 0
    fi
    echo -e "${YELLOW}🧹 Nettoyage en cours...${NC}"
    docker-compose -f $COMPOSE_FILE --env-file $ENV_FILE down -v
    echo -e "${GREEN}✅ Nettoyage terminé${NC}"
}

setup_local() {
    echo -e "${BLUE}🛠️  Configuration locale (sans Docker)${NC}"

    read -p "Souhaites-tu installer PostgreSQL ? (oui/non) " install_pg
    if [[ "$install_pg" == "oui" ]]; then
        echo -e "${YELLOW}📦 Installation PostgreSQL...${NC}"
        sudo dnf install -y postgresql-server postgresql-contrib
        sudo postgresql-setup --initdb
        sudo systemctl enable --now postgresql
        echo -e "${GREEN}✅ PostgreSQL installé${NC}"
    fi

    read -p "Souhaites-tu configurer PostgreSQL (user/db/mdp) ? (oui/non) " config_pg
    if [[ "$config_pg" == "oui" ]]; then
        export DB_USER="upstra"
        export DB_NAME="upstradb"
        export DB_PASSWORD=$(openssl rand -base64 20)
        echo -e "${YELLOW}🔐 Mot de passe généré pour PostgreSQL: ${GREEN}${DB_PASSWORD}${NC}"
        sudo -u postgres psql -c "CREATE USER $DB_USER WITH PASSWORD '${DB_PASSWORD}';"
        sudo -u postgres psql -c "CREATE DATABASE $DB_NAME OWNER $DB_USER;"
        sudo -u postgres psql -c "GRANT ALL PRIVILEGES ON DATABASE $DB_NAME TO $DB_USER;"
        sudo sed -i -E 's/(peer|ident)/md5/g' /var/lib/pgsql/data/pg_hba.conf
        sudo systemctl restart postgresql
        echo -e "${GREEN}✅ PostgreSQL configuré avec succès${NC}"
        echo "→ Pour te connecter : psql -U $DB_USER -d $DB_NAME -h 127.0.0.1 -W"
    fi

    read -p "Souhaites-tu installer Redis ? (oui/non) " install_redis
    if [[ "$install_redis" == "oui" ]]; then
        echo -e "${YELLOW}📦 Installation Redis...${NC}"
        sudo dnf install -y redis
        sudo systemctl enable --now redis
        echo -e "${GREEN}✅ Redis installé${NC}"
    fi

    read -p "Souhaites-tu configurer un mot de passe Redis ? (oui/non) " config_redis
    if [[ "$config_redis" == "oui" ]]; then
        export REDIS_PASSWORD=$(openssl rand -base64 20)
        echo -e "${YELLOW}🔐 Mot de passe généré pour Redis: ${GREEN}${REDIS_PASSWORD}${NC}"
        REDIS_CONF="/etc/redis/redis.conf"
        if grep -q "^# *requirepass" $REDIS_CONF; then
            sudo sed -i "s|^# *requirepass.*|requirepass $REDIS_PASSWORD|" $REDIS_CONF
        elif grep -q "^requirepass" $REDIS_CONF; then
            sudo sed -i "s|^requirepass.*|requirepass $REDIS_PASSWORD|" $REDIS_CONF
        else
            echo "requirepass $REDIS_PASSWORD" | sudo tee -a $REDIS_CONF >/dev/null
        fi
        sudo systemctl restart redis
        echo -e "${GREEN}✅ Redis configuré avec succès${NC}"
        echo "→ Pour te connecter : redis-cli -a $REDIS_PASSWORD"
    fi

    echo -e "${GREEN}🎉 Setup local terminé !${NC}"
}

case "$1" in
    deploy)
        deploy
        ;;
    start)
        start
        ;;
    stop)
        stop
        ;;
    restart)
        restart
        ;;
    status)
        status
        ;;
    logs)
        logs $@
        ;;
    backup)
        backup
        ;;
    restore)
        restore
        ;;
    migrate)
        migrate
        ;;
    clean)
        clean
        ;;
    setup-local)
        setup_local
        ;;
    help|--help|-h|"")
        show_help
        ;;
    *)
        echo -e "${RED}❌ Commande inconnue: $1${NC}"
        show_help
        exit 1
        ;;
esac
